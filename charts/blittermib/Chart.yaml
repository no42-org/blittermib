apiVersion: v2
name: blittermib
description: Pixelperfect SNMP MIB browser — self-hostable, browser-based reference tool.
type: application
# Both version and appVersion are placeholders: `make chart-package`
# stamps BOTH from the release git tag, so the chart's OCI tag always
# equals the blittermib release version (one number for install,
# upgrade, and cosign verify alike).
version: 0.10.0
appVersion: "0.10.0"
home: https://github.com/no42-org/blittermib
sources:
  - https://github.com/no42-org/blittermib
keywords:
  - snmp
  - mib
  - opennms
maintainers:
  - name: Ronny Trommer
    email: ronny@no42.org
