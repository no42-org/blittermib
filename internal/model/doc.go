// Package model defines the normalized in-memory types for parsed
// MIB content: modules, symbols, OID tree nodes, and cross-references.
//
// These types are the boundary between the compile pipeline (which
// produces them) and the store package (which persists them).
package model
